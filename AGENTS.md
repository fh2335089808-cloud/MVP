# 菌鲜到｜云南野生菌订购登记 - 需求拆解文档

## 产品概述

- **产品类型**: 订购登记表单（单页表单应用）
- **场景类型**: <scene_type>prototype-app</scene_type>
- **目标用户**: 有云南野生菌购买需求的消费者（昆明官渡区同城用户、到店自取用户、外地咨询用户）
- **核心价值**: 简洁高效地收集野生菌订购需求信息，由工作人员后续人工确认价格、库存与配送，降低生鲜非标品的交易沟通成本
- **界面语言**: 中文
- **主题偏好**: user_specified（绿色 / 米白色调，简洁自然可信；需消费 Atelier Veda 风格附件的设计语言与组件规范，但色彩体系按用户"绿色或米白色"要求适配，不使用附件的深色主背景）
- **导航模式**: 无导航（单页表单）
- **导航布局**: 无

> **风格适配说明**: 用户提供的风格附件 "Atelier Veda - Spatial Mechanics" 为深色 Hero 模板，其设计语言（字体层级、间距系统、圆角规范、组件质感）需保留并应用于表单；但色彩体系需按用户明确要求的"绿色或米白色、简洁自然可信"进行适配，将 secondary #6F7F63（苔绿）作为主色调，米白色作为背景与表面色，避免使用附件的深色背景模式。

---

## 页面结构总览

> **说明**: 单页表单应用，无导航，无路由跳转

**页面文件**: `OrderFormPage.tsx`

| 区域 | 说明 |
|-----|------|
| 顶部标题区 | 品牌名「菌鲜到」+ 表单标题「云南野生菌订购登记」+ 表单说明文字 |
| 表单主体区 | 12 个表单字段，按逻辑分组（联系信息 / 选购信息 / 配送信息 / 其他需求） |
| 底部提示区 | 3 条重要提示（配送范围、食用安全、表单性质） |
| 提交按钮区 | 提交按钮 + 提交成功后的结果提示 |
| 成功状态层 | 提交成功后展示的确认信息（覆盖或替换表单） |

---

## 页面布局建议

- **布局模式**: 单栏居中布局 —— 表单类应用天然适合单栏，用户自上而下填写，视线流清晰
- **视觉重心**: 表单主体 —— 所有字段按逻辑分组排列，组间有明确间距分隔，提交按钮位于底部醒目位置
- **结果承载区**: 成功状态层（提交成功后替换表单区域，显示确认文案）；初始态为表单本身，无额外结果区
- **表单分组**:
  - 第一组：联系信息（姓名、联系电话、微信号）
  - 第二组：选购信息（需要的野生菌、购买数量、其他数量）
  - 第三组：配送信息（配送方式、官渡区配送地址、期望送达时间、预约日期）
  - 第四组：补充信息（其他需求、渠道来源）

---

## 数据来源声明

| 数据/操作 | 来源类型 | 实现要求 | mock 兜底 |
|---|---|---|---|
| 表单字段选项数据 | demo-mock | `src/data/formOptions.ts` 中定义野生菌品种、数量规格、配送方式、送达时间等静态选项 | ✅ 本身就是 mock（静态配置） |
| 表单提交 | demo-mock | 前端模拟提交（setTimeout + 状态切换），展示成功提示文案；不接入真实后端 | ✅ 本身就是 mock（演示用途） |
| 渠道来源 URL 参数预填 | real-api（浏览器 API） | 页面加载时读取 `URLSearchParams` 中的 `src` 参数，自动填充到渠道来源字段 | 无参数时字段为空 |
| 表单本地草稿保存 | local-persist | `localStorage` key=`__app_junxiandao_form_draft`，用户输入时自动保存草稿，刷新后恢复 | 无草稿时为空表单 |

> 注：本表单为需求登记用途，不直接收款，无真实支付/订单后端对接需求，提交逻辑以演示为主。

---

## 功能列表

- **页面/区块**: 订购登记表单
  - **页面目标**: 让用户快速、准确地填写野生菌订购需求并提交
  - **功能点**:
    - **表单字段渲染与分组**: 按联系信息 / 选购信息 / 配送信息 / 补充信息四组渲染 12 个字段，字段类型严格按需求（单行文本、手机号、多选、单选、多行文本、日期）实现，必填字段标注红星
    - **条件字段联动显示**: 购买数量选"其他数量"时显示「其他数量」输入框；配送方式选"官渡区同城配送"时显示「官渡区配送地址」输入框；期望送达时间选"预约其他日期"时显示「预约日期」选择器；未选中对应选项时相关字段隐藏
    - **表单校验与提交**: 必填项校验（姓名、联系电话、野生菌多选、购买数量、配送方式、期望送达时间），手机号格式校验，校验不通过时字段下方显示错误提示；全部通过后模拟提交（loading 状态 + 成功态切换）
    - **URL 参数预填渠道来源**: 页面初始化时读取 URL query string 中的 `src` 参数，自动填入「渠道来源」字段；该字段默认隐藏或置于表单底部不干扰主流程
    - **提交成功提示**: 提交成功后表单区域替换为成功状态卡片，展示用户指定的确认文案（"登记已收到。野生菌价格和库存随当天市场变化..."）
    - **底部安全与说明提示**: 表单底部以浅底色提示框形式展示 3 条重要提示（配送范围、食用安全、不直接收款），字号略小于正文

---

## 字段规格明细

> 供设计与开发阶段精确实现使用

| 序号 | 字段名 | 字段标识 | 类型 | 必填 | 选项/提示 | 联动条件 |
|-----|-------|---------|------|------|----------|---------|
| 1 | 姓名 | `name` | 单行文本 | 是 | - | - |
| 2 | 联系电话 | `phone` | 手机号 | 是 | 11 位手机号格式校验 | - |
| 3 | 微信号 | `wechat` | 单行文本 | 否 | 提示：方便工作人员联系并发送当天鲜菌图片。 | - |
| 4 | 需要的野生菌 | `mushrooms` | 多选 | 是 | 鸡枞菌 / 青头菌 / 松茸 / 牛肝菌 / 其他当季野生菌 | - |
| 5 | 购买数量 | `quantity` | 单选 | 是 | 500g / 1kg / 2kg / 其他数量 | - |
| 6 | 其他数量 | `otherQuantity` | 单行文本 | 否 | 提示：选择"其他数量"时填写。 | quantity === '其他数量' 时显示 |
| 7 | 配送方式 | `deliveryType` | 单选 | 是 | 官渡区同城配送 / 到店自取 / 外地寄送咨询 | - |
| 8 | 官渡区配送地址 | `deliveryAddress` | 多行文本 | 否 | 提示：选择"官渡区同城配送"时，请填写街道、小区、楼栋和门牌号。当前同城配送范围暂限昆明市官渡区，具体范围和配送费用由工作人员确认。 | deliveryType === '官渡区同城配送' 时显示 |
| 9 | 期望送达时间 | `deliveryTime` | 单选 | 是 | 今天 / 明天 / 预约其他日期 | - |
| 10 | 预约日期 | `appointmentDate` | 日期 | 否 | 提示：选择"预约其他日期"时填写。 | deliveryTime === '预约其他日期' 时显示 |
| 11 | 其他需求 | `remarks` | 多行文本 | 否 | 提示：可填写是否需要处理干净、烹饪建议、送礼包装等需求。 | - |
| 12 | 渠道来源 | `src` | 单行文本 | 否 | 提示：用于统计宣传卡片或推广渠道来源。 | 支持 URL `?src=xxx` 参数预填 |

---

## 风格适配指引

> 用户提供了 Atelier Veda 风格附件，同时明确要求"绿色或米白色、简洁自然可信"。以下为风格消费与适配原则，供 design-agent 阶段执行。

### 必须保留的 Atelier Veda 设计语言

| 设计元素 | 来源规范 | 应用方式 |
|---------|---------|---------|
| **字体体系** | Inter（标题）+ Manrope（正文）+ JetBrains Mono（标签/元数据） | 表单标题用 Inter，字段标签与正文用 Manrope，字段标识/技术说明用 JetBrains Mono |
| **间距系统** | base 8px / gap 16px / card-padding 24px | 表单内边距、字段间距、分组间距严格按 8px 栅格执行 |
| **圆角规范** | card 8px / control 8px / pill 9999px | 输入框、单选/多选框、按钮、卡片统一使用 8px 圆角 |
| **组件质感** | surface token + subtle borders + shadow depth | 表单卡片使用表面色 + 细边框 + 柔和阴影，保持克制的层次感 |
| **动效风格** | masked reveals / staggered entrance / hover lift / smooth easing | 字段聚焦、按钮 hover、成功态切换使用平滑过渡，不做夸张动效 |

### 色彩适配（按用户要求调整）

> 附件为深色模式，但用户明确要求"绿色或米白色"，因此色彩体系需做如下适配：

| 色彩角色 | 附件原值 | 适配后建议值 | 理由 |
|---------|---------|-------------|-----|
| 背景 (background) | `#171611`（深黑） | `#FAF8F3`（米白） | 用户要求米白色背景，自然可信 |
| 表面 (surface) | `#6F7F63`（苔绿） | `#FFFFFF`（纯白）或米白变体 | 表单卡片用浅色底，保证输入可读性 |
| 主色 (primary) | `#171611`（深黑） | `#4A5D42`（深苔绿） | 从附件 secondary 衍生，作为主按钮/强调色 |
| 辅助色 (secondary) | `#6F7F63`（苔绿） | `#6F7F63`（保留） | 用于次要按钮、边框、图标等 |
| 主文字 (text-primary) | `#FFFFFF`（白） | `#171611`（深黑/深棕） | 浅色背景上用深色文字，保留附件的文字色值但反转对比度 |
| 次文字 (text-secondary) | `#A1A1AA`（灰） | `#6B7280`（中性灰） | 提示文字、辅助说明 |
| 边框 (border) | `#4F5D47` | `#D4D8CD`（浅绿灰） | 输入框边框、分隔线 |

> 核心原则：保留附件的 **#6F7F63 苔绿色系** 作为品牌主色调，将背景从深色翻转为米白/浅色模式，形成"绿 + 米白"的自然可信视觉，符合野生菌产品的自然属性。

-------

<scene_type>prototype-app</scene_type>

# UI 设计指南

## 1. 设计推导依据

- **参考意图**: Mood Reference —— 提取 Atelier Veda 的墨绿+深墨色对比气质、克制排版与精细边界感，迁移到野生菌订购表单的自然可信调性中；不照搬深色背景与 WebGL 效果。
- **核心情绪 / 应用类型**: 自然、克制、可信的生鲜订购登记表单，偏向轻量工具型落地页。
- **独特记忆点**: 以松针绿为主交互色、米白宣纸感为底，表单选项用细边卡片式选择器呼应云南山林野生菌的质朴与新鲜。

## 2. Art Direction

- **方向名**: 山林素简
- **Design Style**: Warm Natural 自然暖调 + Swiss Minimalist 瑞士极简 —— 野生菌产品语义需要自然、可信与干净，极简排版保证表单填写效率与信任感。
- **DNA 参数**: 圆角 subtle（8px） / 阴影 subtle（极轻纸感） / 间距 standard（16px 基准） / 字体方向 无衬线中文正文 + 等宽标签 / 装饰手法 细线分隔、浅绿选中底、无多余渐变。
- **应用类型**: Tool / Landing 混合 —— 单列表单居中，顶部标题区带品牌感，底部提示区独立。

## 3. Color System

**色彩关系**: 松针绿主色 + 米白宣纸背景 + 同色系浅绿反馈底 + 深墨文字，整体低饱和、高可信。
**配色设计理由**: primary 松针绿承载主按钮与选中态，传达自然、新鲜、山林气息；bg 米白营造温润纸张感，降低填写压力；accent 浅绿用于 hover 与选中底；border 偏灰绿保持自然统一。
**主色推导**: 从 Atelier Veda 的 secondary #6F7F63（灰绿）出发，提升饱和度与明度至更有生机的松针绿，呼应云南野生菌山林语义，同时保留参考风格的克制灰度气质。
**使用比例**: 65% 中性（米白+深墨）/ 25% 辅助（浅绿、灰绿）/ 10% primary；primary 仅用于主按钮、选中态与关键强调，不滥用在边框、icon 与链接上。

| 角色 | CSS 变量 | Tailwind Class | HSL 值 | 设计说明 |
|---|---|---|---|---|
| bg | `--background` | `bg-background` | hsl(48 30% 96%) | 米白宣纸感页面背景 |
| card | `--card` | `bg-card` | hsl(0 0% 100%) | 表单卡片、结果面板纯白底 |
| text | `--foreground` | `text-foreground` | hsl(30 6% 20%) | 深墨色标题与正文 |
| textMuted | `--muted-foreground` | `text-muted-foreground` | hsl(30 4% 45%) | 提示文字、占位符、辅助说明 |
| primary | `--primary` | `bg-primary` / `text-primary` | hsl(120 22% 35%) | 松针绿，主交互与选中态 |
| primaryForeground | `--primary-foreground` | `text-primary-foreground` | hsl(0 0% 100%) | 主按钮上的白色文字 |
| accent | `--accent` | `bg-accent` | hsl(120 25% 92%) | 浅绿，hover/选中浅底、提示底 |
| accentForeground | `--accent-foreground` | `text-accent-foreground` | hsl(120 22% 30%) | accent 上的深绿文字与图标 |
| border | `--border` | `border-border` | hsl(30 8% 85%) | 输入框、卡片、分隔线边界 |

**语义色提示**: 错误态用于表单校验：bg `hsl(0 40% 96%)` / border `hsl(0 30% 70%)` / text `hsl(0 40% 40%)`，饱和度与 primary 对齐；成功态用于提交结果：bg `hsl(120 25% 92%)` / border `hsl(120 20% 75%)` / text `hsl(120 22% 30%)`，与主色同色系；警告态用于底部食用提示：bg `hsl(40 60% 94%)` / border `hsl(40 40% 70%)` / text `hsl(35 50% 35%)`，暖调与米白背景协调。

## 4. 字体与节奏

- **font-display**: Noto Sans SC —— 中文标题清晰稳重，呼应自然可信的品牌调性；fallback: Inter, system-ui
- **font-body**: Noto Sans SC —— 正文表单标签与说明文字易读性优先；fallback: Manrope, system-ui
- **font-label**: JetBrains Mono —— 用于字段标识、必填标记、渠道参数等技术元信息，呼应参考风格的等宽标签气质
- **字号**: H1 text-2xl ~ text-3xl；字段标签 text-sm font-medium；正文/输入 text-base；muted/提示 text-xs ~ text-sm。
- **圆角**: 小（8px）—— 参考风格 card/control 均为 8px，克制专业。

## 5. 全局布局契约

- **Reference Layout Use**: 按需求结构推导；参考风格影响色彩对比方式、字体层级（display/body/label 三级）与控件密度，不影响表单字段顺序。
- **Page / Section Order**: 顶部品牌标题区 → 表单说明 → 表单字段区（12 个字段按需求顺序） → 提交按钮 → 底部三条提示 → 成功态覆盖层/内联。
- **Standard Content Zone**: 表单容器 `max-w-xl mx-auto`，保证移动端与桌面端填写舒适，不过宽。
- **Shell / Frame Alignment**: 单页无导航，内容容器独立居中，与页面背景同宽但受 max-w 约束。
- **Padding & Rhythm**: `px-4 md:px-6 py-10 md:py-14`，字段间 `gap-5`，区块间 `space-y-8`。
- **Full-bleed Zones**: 页面背景全宽，表单卡片始终受 max-w-xl 约束。
- **Local Narrowing**: 表单本身已收窄至 max-w-xl，内部字段统一单列。
- **Overflow Strategy**: 多选/单选选项组在移动端自动换行，无需横向滚动。
- **Flexibility Boundary**: 允许移动端字段间距、卡片内边距微调；不允许改变主色、圆角、单列布局与字体层级。

## 6. 视觉与动效

- **装饰**: 极细灰绿分隔线、浅绿选中底、顶部可选极简菌菇线描图标（可选）
- **阴影/边界**: 轻 —— 表单卡片 `shadow-sm`，选项卡无边框选中时加 1px 主色边
- **动效**: 克制 —— hover 背景色过渡 150ms，选中态轻微底色变化，提交按钮 loading 态，成功态淡入；无夸张弹跳或位移

## 7. 组件原则

- 按钮、输入框、单选/多选卡必须有 Default / Hover / Active / Focus-visible / Disabled 状态。
- Primary 按钮承担提交；字段标签用 text-sm font-medium；提示文字用 text-muted-foreground text-xs。
- 单选/多选采用卡片式选择器（细边 + 8px 圆角 + 选中浅绿底 + 主色左边框或勾选标记），而非原生 radio/checkbox。
- 条件字段（其他数量、配送地址、预约日期）在对应选项选中时平滑展开，未选中时折叠隐藏。
- 表单校验错误在字段下方以红色小字 + 红色边框提示，不使用弹窗打断。
- 渠道来源 src 字段支持 URL 参数预填，可隐藏或设为只读。

## 8. Image Direction

- **Image Role**: 顶部品牌区小型装饰图 / 无强制图片需求，优先通过排版与色彩建立记忆点
- **Image Art Direction**: 若使用，应为极简风格的野生菌线描或单色水彩插画，松针绿单色调，置于标题左侧或上方，尺寸小、权重低，不抢表单注意力；构图简洁，留白多。
- **Image Prompt Keywords**: 单色线描野生菌插画、松针绿色调、极简留白、鸡枞菌松茸组合、自然手绘感、低对比度、宣纸底纹
- **Image Avoidance**: 避免高饱和彩色电商产品图、避免夸张美食摄影、避免复杂山林风景背景、避免卡通拟人化菌菇形象

## 9. Anti-patterns

- **Promotional drift**: 使用红色促销按钮、倒计时、"限时特惠"等夸张营销语言；本表单需克制可信，全部使用自然绿与中性色。
- **Split personality**: 表单内混用多种圆角或阴影；全站统一 8px 圆角与轻阴影。
- **Mono-hue tyranny**: 主色铺满按钮、边框、图标、链接、提示；primary 只用于主按钮与选中态，其余用 accent 和中性色。
- **Invisible interaction**: 只有 hover 态，缺少 focus-visible；所有可交互元素必须有清晰键盘焦点环。
- **Overcrowded form**: 字段挤在一起或多列布局；保持单列、充足垂直间距，降低填写压力。
- **Status color drift**: 错误/警告色饱和度过高刺眼；语义色饱和度与 primary 对齐 ±15%，保持自然统一。